namespace NCoreUtils.Memory;

internal static class W
{
    public const string DynamicallyCreatedEmplacer = "Emplacer is created dynamically. Explicitly preserving SpanEmplaceableEmplacer<T> may be required.";
}